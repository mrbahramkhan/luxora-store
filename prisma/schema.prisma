generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Data models go here as the storefront, admin, and auth features are implemented.
// See README.md for the intended domain (User, Product, Category, Order, Review, Coupon, etc.)
